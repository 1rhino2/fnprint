[package]
name = "fnprint"
version = "0.3.3"
edition.workspace = true
license.workspace = true
description = "Behavioral function fingerprinting: name functions in stripped binaries and patch-diff builds by what they do, not their bytes"
repository.workspace = true
documentation = "https://docs.rs/fnprint"
keywords = ["reverse-engineering", "binary-analysis", "fingerprint", "malware", "security"]
categories = ["command-line-utilities", "development-tools", "development-tools::debugging"]
readme = "../README.md"

[[bin]]
name = "fnprint"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
fnprint-core = { path = "../crates/fnprint-core", version = "0.3.2" }
fnprint-db = { path = "../crates/fnprint-db", version = "0.3.2" }
fnprint-loader = { path = "../crates/fnprint-loader", version = "0.3.2" }
fnprint-emu = { path = "../crates/fnprint-emu", version = "0.3.2" }
fnprint-sandbox = { path = "../crates/fnprint-sandbox", version = "0.3.2" }
serde = { workspace = true }
postcard = { workspace = true }
# safe syscall wrapper (already in the tree transitively) for the process-group
# kill in the worker watchdog. keeps the cli unsafe-forbid, no raw libc::kill.
rustix = { version = "0.38", features = ["process"] }

[lints]
workspace = true
